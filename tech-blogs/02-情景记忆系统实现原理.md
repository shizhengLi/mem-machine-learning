# 情景记忆系统实现原理

## 概述

情景记忆系统（Episodic Memory System）是MemMachine的核心组件之一，负责管理AI智能体的对话历史和上下文信息。本文基于实际源代码深入分析其实现原理、核心算法和设计模式。

## 系统架构

### 双层记忆结构

情景记忆系统采用双层架构设计：

1. **短期记忆（Short-Term Memory）**：当前会话的临时信息
2. **长期记忆（Long-Term Memory）**：持久化的对话历史

### 核心组件关系

```
EpisodicMemoryManager
    ├── EpisodicMemory (会话实例)
    │   ├── ShortTermMemory (短期记忆)
    │   └── LongTermMemory (长期记忆)
    └── EpisodeStorage (持久化存储)
```

## 核心数据模型

### Episode完整模型

基于`src/memmachine/common/episode_store/episode_model.py`：

```python
from enum import Enum
from pydantic import AwareDatetime, BaseModel, JsonValue

class EpisodeType(Enum):
    """对话条目类型"""
    MESSAGE = "message"

class ContentType(Enum):
    """内容类型"""
    STRING = "string"

class Episode(BaseModel):
    """完整的对话消息模型"""
    uid: EpisodeIdT                 # 唯一标识符
    content: str                    # 消息内容
    session_key: str                # 会话键
    created_at: AwareDatetime       # 创建时间
    producer_id: str                # 生产者ID
    producer_role: str              # 生产者角色
    produced_for_id: str | None     # 目标用户ID
    sequence_num: int = 0           # 序列号
    episode_type: EpisodeType = EpisodeType.MESSAGE
    content_type: ContentType = ContentType.STRING
    filterable_metadata: dict[str, FilterablePropertyValue] | None = None
    metadata: dict[str, JsonValue] | None = None

    def __hash__(self) -> int:
        """基于UID的哈希函数，用于去重"""
        return hash(self.uid)
```

### EpisodeEntry创建模型

```python
class EpisodeEntry(BaseModel):
    """创建新对话条目的数据结构"""
    content: str                    # 对话内容
    producer_id: str               # 生产者ID
    producer_role: str             # 生产者角色
    produced_for_id: str | None    # 目标用户ID
    episode_type: EpisodeType | None = None
    metadata: dict[str, JsonValue] | None = None
    created_at: AwareDatetime | None = None
```

## EpisodicMemory核心类

### 初始化和配置

基于`src/memmachine/episodic_memory/episodic_memory.py`的实际实现：

```python
import asyncio
import logging
import time
from pydantic import BaseModel, Field, InstanceOf

class EpisodicMemoryParams(BaseModel):
    """情景内存配置参数"""
    session_key: str = Field(
        ...,
        min_length=1,
        description="会话的唯一标识符",
    )
    metrics_factory: InstanceOf[MetricsFactory] = Field(
        ...,
        description="指标工厂",
    )
    long_term_memory: InstanceOf[LongTermMemory] | None = Field(
        default=None,
        description="长期内存",
    )
    short_term_memory: InstanceOf[ShortTermMemory] | None = Field(
        default=None,
        description="短期内存",
    )
    enabled: bool = Field(
        default=True,
        description="是否启用情景内存",
    )

    @model_validator(mode="after")
    def validate_memory_params(self) -> "EpisodicMemoryParams":
        """验证内存参数"""
        if not self.enabled:
            return self
        if self.short_term_memory is None and self.long_term_memory is None:
            raise ValueError(
                "必须提供至少短期内存或长期内存中的一个。",
            )
        return self

class EpisodicMemory:
    """为特定会话提供独立的内存实例"""

    def __init__(self, params: EpisodicMemoryParams) -> None:
        """初始化情景内存实例"""
        self._closed = False
        self._session_key = params.session_key
        self._short_term_memory: ShortTermMemory | None = params.short_term_memory
        self._long_term_memory: LongTermMemory | None = params.long_term_memory
        self._enabled = params.enabled

        if not self._enabled:
            return

        if self._short_term_memory is None and self._long_term_memory is None:
            raise ValueError("No memory is configured")

        # 初始化性能指标
        metrics_manager = params.metrics_factory
        self._ingestion_latency_summary = metrics_manager.get_summary(
            "Ingestion_latency",
            "Latency of Episode ingestion in milliseconds",
        )
        self._query_latency_summary = metrics_manager.get_summary(
            "query_latency",
            "Latency of query processing in milliseconds",
        )
        self._ingestion_counter = metrics_manager.get_counter(
            "Ingestion_count",
            "Count of Episode ingestion",
        )
        self._query_counter = metrics_manager.get_counter(
            "query_count",
            "Count of query processing",
        )
```

## 核心功能实现

### 1. 对话条目添加

```python
async def add_memory_episodes(self, episodes: list[Episode]) -> None:
    """将新的内存条目添加到会话和声明性内存"""

    if not self._enabled:
        return

    start_time = time.monotonic_ns()

    if self._closed:
        raise RuntimeError(f"Memory is closed {self._session_key}")

    # 数据预处理：创建可过滤属性
    for episode in episodes:
        if episode.metadata is not None and episode.filterable_metadata is None:
            episode.filterable_metadata = {}
            for key, value in episode.metadata.items():
                if isinstance(value, get_args(FilterablePropertyValue)):
                    episode.filterable_metadata[key] = value

    # 并发添加到两个内存存储
    tasks: list[Coroutine] = []
    if self._short_term_memory:
        tasks.append(self._short_term_memory.add_episodes(episodes))
    if self._long_term_memory:
        tasks.append(self._long_term_memory.add_episodes(episodes))

    await asyncio.gather(*tasks)

    # 记录性能指标
    end_time = time.monotonic_ns()
    delta = (end_time - start_time) / 1000000  # 转换为毫秒
    self._ingestion_latency_summary.observe(delta)
    self._ingestion_counter.increment()
```

**关键实现细节：**

1. **数据预处理**：自动提取可过滤的元数据到专门字段
2. **并发写入**：同时写入短期和长期记忆存储
3. **性能监控**：记录延迟和计数指标
4. **错误处理**：检查内存状态，防止在关闭状态下操作

### 2. 内存查询实现

```python
class QueryResponse(BaseModel):
    """来自长期和短期内存的聚合搜索结果"""

    class ShortTermMemoryResponse(BaseModel):
        """来自短期内存的聚合搜索结果"""
        episodes: list[EpisodeResponse]
        episode_summary: list[str]

    class LongTermMemoryResponse(BaseModel):
        """来自长期内存的聚合搜索结果"""
        episodes: list[EpisodeResponse]

    long_term_memory: LongTermMemoryResponse
    short_term_memory: ShortTermMemoryResponse

async def query_memory(
    self,
    query: str,
    limit: int | None = None,
    property_filter: FilterExpr | None = None,
) -> QueryResponse | None:
    """从所有内存存储中检索给定查询的相关上下文"""

    if not self._enabled:
        return None

    start_time = time.monotonic_ns()
    search_limit = limit if limit is not None else 20

    # 处理不同的内存配置情况
    if self._short_term_memory is None:
        # 只有长期内存
        short_episode: list[Episode] = []
        short_summary = ""
        long_episode = await cast("LongTermMemory", self._long_term_memory).search(
            query,
            num_episodes_limit=search_limit,
            property_filter=property_filter,
        )

    elif self._long_term_memory is None:
        # 只有短期内存
        session_result = (
            await self._short_term_memory.get_short_term_memory_context(
                query,
                limit=search_limit,
                filters=property_filter,
            )
        )
        long_episode = []
        short_episode, short_summary = session_result

    else:
        # 并发搜索两个内存存储
        session_result, long_episode = await asyncio.gather(
            self._short_term_memory.get_short_term_memory_context(
                query,
                limit=search_limit,
                filters=property_filter,
            ),
            self._long_term_memory.search(
                query,
                num_episodes_limit=search_limit,
                property_filter=property_filter,
            ),
        )
        short_episode, short_summary = session_result

    # 去重处理 - 优先保留短期内存中的条目
    episode_uid_set = {episode.uid for episode in short_episode}

    unique_long_episodes = []
    for episode in long_episode:
        if episode.uid not in episode_uid_set:
            episode_uid_set.add(episode.uid)
            unique_long_episodes.append(episode)

    # 记录性能指标
    end_time = time.monotonic_ns()
    delta = (end_time - start_time) / 1000000
    self._query_latency_summary.observe(delta)
    self._query_counter.increment()

    # 返回结构化的查询结果
    return EpisodicMemory.QueryResponse(
        short_term_memory=EpisodicMemory.QueryResponse.ShortTermMemoryResponse(
            episodes=[
                EpisodeResponse(**episode.model_dump()) for episode in short_episode
            ],
            episode_summary=[short_summary],
        ),
        long_term_memory=EpisodicMemory.QueryResponse.LongTermMemoryResponse(
            episodes=[
                EpisodeResponse(**episode.model_dump())
                for episode in unique_long_episodes
            ],
        ),
    )
```

**查询优化策略：**

1. **并发搜索**：同时查询短期和长期记忆
2. **智能去重**：基于UID去重，优先短期记忆
3. **灵活配置**：支持仅启用一种内存类型
4. **性能监控**：记录查询延迟和次数

### 3. 上下文格式化

```python
async def formalize_query_with_context(
    self,
    query: str,
    limit: int | None = None,
    property_filter: FilterExpr | None = None,
) -> str:
    """构建包含内存上下文的最终查询字符串"""

    # 获取查询结果
    query_result = await self.query_memory(
        query,
        limit,
        property_filter,
    )
    if query_result is None:
        logger.warning("Query result is None in formalize_query_with_context")
        return query

    # 按时间排序所有对话条目
    episodes = sorted(
        query_result.short_term_memory.episodes
        + query_result.long_term_memory.episodes,
        key=lambda x: cast(datetime, x.created_at),
    )

    finalized_query = ""

    # 添加会话总结
    if (
        query_result.short_term_memory.episode_summary
        and len(query_result.short_term_memory.episode_summary) > 0
    ):
        total_summary = ""
        for summ in query_result.short_term_memory.episode_summary:
            if not summ:
                continue
            total_summary = total_summary + summ + "\n"
        total_summary = total_summary.strip()
        if total_summary:
            finalized_query += "<Summary>\n"
            finalized_query += total_summary
            finalized_query += "\n</Summary>\n"

    # 添加相关对话
    if episodes and len(episodes) > 0:
        finalized_query += "<Episodes>\n"
        for episode in episodes:
            # 确保内容是字符串类型
            if isinstance(episode.content, str):
                finalized_query += episode.content
                finalized_query += "\n"
        finalized_query += "</Episodes>\n"

    # 添加原始查询
    finalized_query += f"<Query>\n{query}\n</Query>"

    return finalized_query
```

**上下文增强特性：**
- **XML格式化**：使用XML标签结构化上下文
- **时序排序**：按时间顺序组织对话历史
- **总结优先**：先显示会话总结，再显示具体对话
- **类型安全**：确保内容为字符串类型

## 存储抽象层

### EpisodeStorage接口

基于`src/memmachine/common/episode_store/episode_storage.py`：

```python
from abc import ABC, abstractmethod
from pydantic import AwareDatetime

class EpisodeStorage(ABC):
    """对话历史持久化和检索的抽象接口"""

    @abstractmethod
    async def startup(self) -> None:
        """启动存储服务"""
        raise NotImplementedError

    @abstractmethod
    async def add_episodes(
        self,
        session_key: str,
        episodes: list[EpisodeEntry],
    ) -> list[Episode]:
        """添加对话条目"""
        raise NotImplementedError

    @abstractmethod
    async def get_episode(
        self,
        history_id: EpisodeIdT,
    ) -> Episode | None:
        """获取单个对话条目"""
        raise NotImplementedError

    @abstractmethod
    async def get_episode_messages(
        self,
        *,
        page_size: int | None = None,
        page_num: int | None = None,
        filter_expr: FilterExpr | None = None,
        start_time: AwareDatetime | None = None,
        end_time: AwareDatetime | None = None,
    ) -> list[Episode]:
        """分页获取对话消息"""
        raise NotImplementedError

    @abstractmethod
    async def get_episode_messages_count(
        self,
        *,
        filter_expr: FilterExpr | None = None,
        start_time: AwareDatetime | None = None,
        end_time: AwareDatetime | None = None,
    ) -> int:
        """获取对话消息数量"""
        raise NotImplementedError

    @abstractmethod
    async def delete_episodes(
        self,
        episode_ids: list[EpisodeIdT],
    ) -> None:
        """删除对话条目"""
        raise NotImplementedError

    @abstractmethod
    async def delete_episode_messages(
        self,
        *,
        filter_expr: FilterExpr | None = None,
        start_time: AwareDatetime | None = None,
        end_time: AwareDatetime | None = None,
    ) -> None:
        """按条件删除对话消息"""
        raise NotImplementedError
```

## EpisodicMemoryManager

### 管理器实现

```python
class EpisodicMemoryManager:
    """情景内存管理器"""

    def __init__(self, config: EpisodicMemoryConfig, resource_manager: ResourceManagerImpl):
        self._config = config
        self._resource_manager = resource_manager
        self._memory_instances = {}  # 缓存内存实例

    async def open_or_create_episodic_memory(
        self,
        session_key: str,
        description: str,
        episodic_memory_config: EpisodicMemoryConf,
        metadata: dict[str, Any],
    ) -> AsyncContextManager[EpisodicMemory]:
        """打开或创建情景内存实例"""

        class EpisodicMemoryContext:
            def __init__(self, manager: 'EpisodicMemoryManager', session_key: str):
                self._manager = manager
                self._session_key = session_key

            async def __aenter__(self) -> EpisodicMemory:
                return await self._manager._get_or_create_memory(self._session_key)

            async def __aexit__(self, exc_type, exc_val, exc_tb):
                await self._manager._release_memory(self._session_key)

        return EpisodicMemoryContext(self, session_key)

    async def _get_or_create_memory(self, session_key: str) -> EpisodicMemory:
        """获取或创建内存实例"""
        if session_key not in self._memory_instances:
            # 创建新的内存实例
            memory = await self._create_episodic_memory(session_key)
            self._memory_instances[session_key] = {
                'instance': memory,
                'ref_count': 1,
            }
        else:
            # 增加引用计数
            self._memory_instances[session_key]['ref_count'] += 1

        return self._memory_instances[session_key]['instance']

    async def _release_memory(self, session_key: str):
        """释放内存实例"""
        if session_key in self._memory_instances:
            memory_info = self._memory_instances[session_key]
            memory_info['ref_count'] -= 1

            if memory_info['ref_count'] <= 0:
                # 关闭内存实例
                await memory_info['instance'].close()
                del self._memory_instances[session_key]
```

## 生命周期管理

### 资源清理

```python
async def close(self) -> None:
    """关闭内存实例，清理底层内存存储"""
    self._closed = True
    if not self._enabled:
        return

    tasks = []
    if self._short_term_memory:
        tasks.append(self._short_term_memory.close())
    if self._long_term_memory:
        tasks.append(self._long_term_memory.close())

    await asyncio.gather(*tasks)
```

### 删除操作

```python
async def delete_episodes(self, uids: Iterable[str]) -> None:
    """按UID删除对话条目"""
    if not self._enabled:
        return

    uids = list(uids)

    delete_episodes_coroutines: list[Coroutine] = []
    if self._short_term_memory:
        delete_episodes_coroutines.extend(
            self._short_term_memory.delete_episode(uid) for uid in uids
        )
    if self._long_term_memory:
        delete_episodes_coroutines.append(
            self._long_term_memory.delete_episodes(uids)
        )

    await asyncio.gather(*delete_episodes_coroutines)

async def delete_session_episodes(self) -> None:
    """删除当前会话的所有数据"""
    if not self._enabled:
        return

    tasks = []
    if self._short_term_memory:
        tasks.append(self._short_term_memory.clear_memory())
    if self._long_term_memory:
        tasks.append(self._long_term_memory.delete_matching_episodes())

    await asyncio.gather(*tasks)
```

## 性能优化特性

### 1. 异步并发处理

```python
# 并发写入多个存储系统
tasks: list[Coroutine] = []
if self._short_term_memory:
    tasks.append(self._short_term_memory.add_episodes(episodes))
if self._long_term_memory:
    tasks.append(self._long_term_memory.add_episodes(episodes))
await asyncio.gather(*tasks)

# 并发删除操作
delete_episodes_coroutines: list[Coroutine] = []
if self._short_term_memory:
    delete_episodes_coroutines.extend(
        self._short_term_memory.delete_episode(uid) for uid in uids
    )
if self._long_term_memory:
    delete_episodes_coroutines.append(
        self._long_term_memory.delete_episodes(uids)
    )
await asyncio.gather(*delete_episodes_coroutines)
```

### 2. 智能去重

```python
# 去重处理 - 优先保留短期内存中的条目
episode_uid_set = {episode.uid for episode in short_episode}

unique_long_episodes = []
for episode in long_episode:
    if episode.uid not in episode_uid_set:
        episode_uid_set.add(episode.uid)
        unique_long_episodes.append(episode)
```

### 3. 内置监控

```python
# 初始化性能指标
self._ingestion_latency_summary = metrics_manager.get_summary(
    "Ingestion_latency",
    "Latency of Episode ingestion in milliseconds",
)
self._query_latency_summary = metrics_manager.get_summary(
    "query_latency",
    "Latency of query processing in milliseconds",
)
self._ingestion_counter = metrics_manager.get_counter(
    "Ingestion_count",
    "Count of Episode ingestion",
)
self._query_counter = metrics_manager.get_counter(
    "query_count",
    "Count of query processing",
)

# 记录性能指标
end_time = time.monotonic_ns()
delta = (end_time - start_time) / 1000000
self._ingestion_latency_summary.observe(delta)
self._ingestion_counter.increment()
```

## 配置系统

### EpisodicMemoryConf配置

基于`src/memmachine/common/configuration/episodic_config.py`：

```python
class EpisodicMemoryConf(BaseModel):
    """情景内存配置"""
    session_key: str
    long_term_memory: LongTermMemoryConf | None = None
    short_term_memory: ShortTermMemoryConf | None = None
    long_term_memory_enabled: bool = True
    short_term_memory_enabled: bool = True
    enabled: bool = True

class LongTermMemoryConf(BaseModel):
    """长期内存配置"""
    session_id: str
    vector_graph_store: str
    embedder: str
    reranker: str | None = None

class ShortTermMemoryConf(BaseModel):
    """短期内存配置"""
    session_key: str
    llm_model: str
    summary_prompt_system: str
    summary_prompt_user: str
```

## 总结

基于实际源代码分析，MemMachine的情景记忆系统展现了以下设计特点：

1. **双层架构**：清晰的短期/长期内存分离
2. **异步优化**：全面的并发处理设计
3. **类型安全**：使用Pydantic确保数据完整性
4. **性能监控**：内置详细的性能指标收集
5. **灵活配置**：支持单独启用不同类型的内存
6. **存储抽象**：通过接口支持多种存储后端
7. **上下文增强**：智能的查询上下文格式化
8. **生命周期管理**：完善的资源管理机制

这种设计使得情景记忆系统能够高效地为AI智能体提供连续的对话上下文，同时保持良好的性能和可维护性。